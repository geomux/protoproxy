"""Router — receives messages from any input and dispatches them to the configured output(s)."""


class Router:
    def __init__(self, config: dict):
        pass

    def route(self, message: dict):
        pass
